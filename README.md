# poste-receive-hook-py

- put `post-receive` in the `hooks` directory

- Windows: create a `hooks\post-receive.bat`

- Linux: execute the following commands

```sh
dos2unix hooks/post-receive
chmod +x hooks/post-receive
```
